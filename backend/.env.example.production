# Production Environment Configuration for SuperHosting
# Copy this file to backend/.env and fill in your actual values

# Force discrete DB configuration (required for Neon/external databases)
FORCE_DISCRETE_DB_CONFIG=true

# Neon PostgreSQL Database Configuration
# IMPORTANT: Use DIRECT host (not pooler) for best compatibility with shared hosting
DB_HOST=ep-noisy-pine-agnly9s.eu-central-1.aws.neon.tech
DB_PORT=5432
DB_DATABASE=neondb
DB_USERNAME=neondb_owner
DB_PASSWORD=npg_0vJMr3AQxozV
DB_SSLMODE=require

# Optional: DB_OPTIONS for manual endpoint override
# Uncomment ONLY if you see "Endpoint ID is not specified" errors
# The code auto-detects Neon hosts and injects endpoint automatically
# DB_OPTIONS=options=endpoint=ep-noisy-pine-agnly9s;channel_binding=disable

# Application Settings
APP_ENV=production
APP_DEBUG=false
APP_URL=https://consultingg.com
PUBLIC_BASE_URL=https://consultingg.com

# JWT Authentication
JWT_SECRET=consultingg-jwt-secret-key-production-2024
JWT_AUD=consultingg.com

# Supabase (Frontend only - optional)
SUPABASE_URL=https://gtvcakkgqlpfdivpejmi.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imd0dmNha2tncWxwZmRpdnBlam1pIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTg3MzI5MDAsImV4cCI6MjA3NDMwODkwMH0.YCRquF6NjAl0wAl5I3uNUyxKh0yKyHirjBKKRW3E4Fk

# SuperHosting Deployment Notes:
# 
# 1. PHP Extensions Required (enable in cPanel → Select PHP Version):
#    - pdo_pgsql (PostgreSQL PDO driver)
#    - pgsql (PostgreSQL extension)
#    - mbstring (for Bulgarian text)
#    - json
#    - fileinfo
#
# 2. Neon Connection:
#    - The app auto-detects .neon.tech hosts and injects SNI endpoint
#    - Use DIRECT host (ep-noisy-pine-agnly9s.eu-central-1.aws.neon.tech)
#    - Pooler host works but may need DB_OPTIONS with endpoint override
#
# 3. Health Check Endpoints:
#    - /backend/db_test.php - Tests database connection
#    - /backend/db_neon_check.php - Neon-specific connection test
#    - /api/ - API health check
#
# 4. Troubleshooting:
#    - If "Endpoint ID is not specified": uncomment DB_OPTIONS above
#    - If "password authentication failed": verify Neon credentials in dashboard
#    - If "pdo_pgsql not found": enable extension in cPanel PHP settings
#
# 5. File Permissions:
#    - Ensure /uploads directory is writable (755 or 775)
#    - Set proper ownership for web server user
